---
import { cn } from "@/lib/utils";
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'caption'> {}

const { class: aClass, ...rest } = Astro.props;
---
<caption
  class={cn("mt-4 text-sm text-muted-foreground", aClass)}
  {...rest}
>
  <slot />
</caption>