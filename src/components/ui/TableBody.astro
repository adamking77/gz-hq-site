---
import { cn } from "@/lib/utils";
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'tbody'> {}

const { class: aClass, ...rest } = Astro.props;
---
<tbody class={cn("[&_tr:last-child]:border-0", aClass)} {...rest}>
  <slot />
</tbody>