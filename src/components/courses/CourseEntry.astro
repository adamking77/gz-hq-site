---
const { url, title, description, pubDate, image, alt } = Astro.props;
import Text from "@/components/fundations/elements/Text.astro";
---
<a href={url} title={title} class="group block border border-black/10 dark:border-white/10 rounded-xl transition-all duration-300 ease-in-out hover:scale-105 hover:shadow-lg">
  <div class="flex gap-8 p-10 bg-backgroundLight dark:bg-black rounded-xl"> {/* Removed hover effects from inner div, padding adjusted if needed */}
    <div class="flex-shrink-0 w-40 h-40 rounded-lg overflow-hidden">
      <img
        class="object-cover w-full h-full"
        src={image}
        alt={alt}
      />
    </div>
    <div class="flex flex-col flex-1">
      <Text
        tag="h3"
        variant="textLG"
        class="text-[#222222] dark:text-white font-medium group-hover:text-black/70 dark:group-hover:text-white/70 leading-tight"
      >
        {title}
      </Text>
      <Text
        tag="p"
        variant="textSM"
        class="text-borderAlt dark:text-borderAlt text-balance line-clamp-2 mt-4"
      >
        {description}
      </Text>
      <Text
        tag="p"
        variant="textSM"
        class="text-accent dark:text-accent group-hover:text-accent/80 dark:group-hover:text-accent/80 mt-4 font-medium"
      >
        View Course →
      </Text>
    </div>
  </div>
</a>